---
layout: layouts/base.njk
section: post
---
{% include "components/hero.njk" %}
<section class="article">
<article>
  <!--img class="cover" src="{{ cover }}"><h1>{{ title }}</h1--> 
  <p>
    <small>
      <time datetime="{{ page.date | machineDate }}">{{ page.date | readableDate }}</time> by <a href="/authors/{{ author | slug }}/">{{ author }}</a>
    </small>
  </p>

  {{ layoutContent | safe }}

  {% if tags %}
    <p>
    {% for tag in tags %}
      {%- if tag != "post" -%}
        {% set tagUrl %}/tags/{{ tag }}/{% endset %}
        <a href="{{ tagUrl | url }}" rel="tag">{{ tag }}</a>
      {%- endif -%}
    {% endfor %}
    </p>
  {% endif %}

</article>
</section>
<nav>
  <a href="{{ '/blog/' | url }}">← Blog index</a>
</nav>
<style scoped>.article{
background-color: var(--background);
    /* display: grid; */
    margin-top: 16rem;
    /* position: relative; */
    
    }

    article > *{max-width:100vw}
  </style>