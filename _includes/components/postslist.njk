<section class="flex">
	{% for post in postslist | reverse %}
		<article{% if post.url == url %} data-current="current item"{% endif %}>
			<h3>
				<div class="image-shadow js-tilt-container" style="background-image:url({{ post.data.cover }})">
				<!--img class="cover" src="{{ post.data.cover }}"-->
				<a href="{{ post.url | url }}">
					{% if post.data.title %}
						{{ post.data.title }}
					{% else %}
						Untitled
					{% endif %}
				</a>
			</h3>
			{% if post.data.summary %}
				<p>
					{{ post.data.summary }}
				</p>
			{% endif %}
			<p>
				<time datetime="{{ post.date | machineDate }}">
					<small>{{ post.date | readableDate }} by <a href="/authors/{{ post.data.author | slug }}/">{{ post.data.author }}</a></small>
				</time>
			</p>
			{% if post.data.tags %}
				<p>
					{% for tag in post.data.tags %}
		  			{%- if tag != "post" -%}
		  				{% set tagUrl %}/tags/{{ tag }}/{% endset %}
		  			<a href="{{ tagUrl | url }}" rel="tag">{{ tag }}</a>
		  			{%- endif -%}
					{% endfor %}
				</p>
			{% endif %}
		</article>
	{% endfor %}
</section>
<style scoped>
section {
    display: grid;
    flex-wrap: nowrap;
    justify-items: start;
    align-items: start;
    justify-content: space-around;
    align-content: center;
}

.image-shadow:after {
  transform: scale(0.95) translateY(36px) translateZ(-30px);
  filter: blur(20px);
  opacity: 0.9;
  content: '';
  position: absolute;
  width: 100%;
  height: 100%;
  background-image: inherit;
  background-size: cover;
  z-index: -1;
  transition: filter .3s ease;
}

.image-shadow {
	    background: var(--secondary);
  margin: 20px auto;
  width: 400px;
  height: 200px;
  border-radius: 10px;
  display: block;
  position: relative;
  background-size: cover;
}
	</style>