<section class="flex">
	{% for post in postslist | reverse %}
		<article{% if post.url == url %} data-current="current item"{% endif %} class="4-col">
			<h3><a href="{{ post.url | url }}">
				<div class="image-shadow js-tilt-container" style="background-image:url({{ post.data.cover }})">
				<h2 class="cardtitle">
				
					{% if post.data.title %}
						{{ post.data.title }}
					{% else %}
						Untitled
					{% endif %}
					</h2>
				</a>
			</h3>
			{% if post.data.summary %}
				<p>
					{{ post.data.summary }}
				</p>
			{% endif %}
			<p>
				<time datetime="{{ post.date | machineDate }}">
					<small>{{ post.date | readableDate }} by <a href="/authors/{{ post.data.author | slug }}/">{{ post.data.author }}</a></small>
				</time>
			</p>
			{% if post.data.tags %}
				<p>
					{% for tag in post.data.tags %}
		  			{%- if tag != "post" -%}
		  				{% set tagUrl %}/tags/{{ tag }}/{% endset %}
		  			<a href="{{ tagUrl | url }}" rel="tag">{{ tag }}</a>
		  			{%- endif -%}
					{% endfor %}
				</p>
			{% endif %}
		</article>
	{% endfor %}
</section>
<style scoped>
section {
   
}


/* .image-shadow:after {
  transform: scale(0.95) translateY(36px) translateZ(-30px);
  filter: blur(20px);
  opacity: 0.9;
  content: '';
  position: absolute;
  width: 100%;
  height: 100%;
  background-image: inherit;
  background-size: cover;
  z-index: -1;
  transition: filter .3s ease;
}*/
.image-shadow, .image-shadow::hover, .image-shadow::before{border-radius:12px}  
.image-shadow:hover{  transform: scale(1.25)}
.image-shadow {
	    background: var(--secondary);
  margin: 20px auto;
  width: 560px;
  height: 200px;
  display: block;
  position: relative;
  background-size: cover;
}
.image-shadow::before {
  background-color: var(--primary);
  content: '';
  display: block;
  width: 100%;
  height: 100%;
  mix-blend-mode: darken;
  position: absolute;
  top: 0;
  left: 0;
}

.image-shadow::after {
  background-color: var(--text-copy-primary);
  content: '';
  display: block;
  width: 100%;
  height: 100%;
  mix-blend-mode: lighten;
  position: absolute;
  top: 0;
  left: 0;
}

/* card title*/
h2.cardtitle {
    position: relative;
    top: 20px;
    left: 20px;
    color: white;
}
	</style>